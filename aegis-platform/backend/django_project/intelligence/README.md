# Threat Intelligence Domain

The intelligence domain persists immutable snapshots of live provider enrichment with source URLs, provider failures, confidence, conflict indicators and SHA-256 provenance hashes.
