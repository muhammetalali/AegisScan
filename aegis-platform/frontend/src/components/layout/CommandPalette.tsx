import { useEffect, useMemo, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { Search, LayoutDashboard, FolderKanban, Server, Play, Bug, FileText, ShieldCheck, TrendingUp, GitBranch, BookOpen, Users, ClipboardList, Bell, Settings, Monitor, Zap, Database } from 'lucide-react'
import { useLanguageStore } from '@/stores/languageStore'

const ITEMS=[{label:'Dashboard',href:'/dashboard',icon:LayoutDashboard,kws:'home overview'},{label:'New Validation',href:'/validations/new',icon:Zap,kws:'scan validation url ip code api'},{label:'Projects',href:'/projects',icon:FolderKanban,kws:'project'},{label:'Assets',href:'/assets',icon:Server,kws:'asset host domain'},{label:'Findings',href:'/vulnerabilities',icon:Bug,kws:'vulnerability finding'},{label:'Evidence',href:'/evidence',icon:Database,kws:'evidence provenance sha256'},{label:'Reports',href:'/reports',icon:FileText,kws:'report pdf export'},{label:'Compliance',href:'/compliance',icon:ShieldCheck,kws:'nist iso pci'},{label:'Security Posture',href:'/posture',icon:TrendingUp,kws:'posture risk trend'},{label:'Digital Twin',href:'/digital-twin',icon:GitBranch,kws:'twin simulation'},{label:'Knowledge',href:'/knowledge',icon:BookOpen,kws:'knowledge'},{label:'Users & RBAC',href:'/users',icon:Users,kws:'user role'},{label:'Audit Trail',href:'/audit',icon:ClipboardList,kws:'audit log'},{label:'Notifications',href:'/notifications',icon:Bell,kws:'notification'},{label:'Settings',href:'/settings',icon:Settings,kws:'setting'},{label:'System Monitoring',href:'/system',icon:Monitor,kws:'system health'},{label:'Validations',href:'/scan',icon:Play,kws:'scan execution validation run'}]
export const CommandPalette=({open,onOpenChange}:{open:boolean;onOpenChange:(v:boolean)=>void})=>{const t=useLanguageStore(s=>s.t);const[q,setQ]=useState('');const navigate=useNavigate();const filtered=useMemo(()=>{if(!q)return ITEMS;const l=q.toLowerCase();return ITEMS.filter(i=>i.label.toLowerCase().includes(l)||i.kws.includes(l)||i.href.includes(l))},[q]);useEffect(()=>{const h=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();onOpenChange(!open)}if(e.key==='Escape')onOpenChange(false)};window.addEventListener('keydown',h);return()=>window.removeEventListener('keydown',h)},[open,onOpenChange]);useEffect(()=>{if(!open)setQ('')},[open]);if(!open)return null;return <div className="fixed inset-0 z-[100] flex items-start justify-center pt-[16vh]" aria-modal="true"><div className="absolute inset-0 bg-black/55 backdrop-blur-md" onClick={()=>onOpenChange(false)}/><div className="relative w-full max-w-2xl overflow-hidden rounded-2xl border border-border/70 bg-card/95 shadow-2xl backdrop-blur-2xl"><div className="flex items-center gap-2 border-b px-4 py-3"><Search className="h-4 w-4 text-muted-foreground"/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder={t('Search pages, validations, findings...')} className="flex-1 bg-transparent outline-none text-sm"/><span className="rounded border bg-muted px-1.5 py-0.5 text-xs">ESC</span></div><div className="max-h-[55vh] overflow-auto p-2">{filtered.length===0?<div className="py-10 text-center text-sm text-muted-foreground">{t('No results')}</div>:filtered.map(item=><button key={item.href} onClick={()=>{onOpenChange(false);navigate(item.href)}} className="flex w-full items-center gap-3 rounded-xl px-3 py-3 text-start transition hover:bg-accent"><item.icon className="h-4 w-4 text-muted-foreground"/><span className="text-sm font-medium">{t(item.label)}</span><span className="ms-auto font-mono text-[10px] text-muted-foreground">{item.href}</span></button>)}</div><div className="flex justify-between border-t bg-muted/20 px-3 py-2 text-xs text-muted-foreground"><span>{t('Navigation')}</span><span>AegisScan</span></div></div></div>}
