import { useState } from 'react'
import { Link } from 'react-router-dom'
import { useQuery, useQueryClient } from '@tanstack/react-query'
import { Activity, AlertCircle, ArrowUpRight, Play, Plus, RefreshCw, Trash2, X } from 'lucide-react'
import { apiHelpers } from '@/services/api'
import { useLanguageStore } from '@/stores/languageStore'
import { toast } from 'sonner'
import { cn } from '@/utils/cn'

type Scan={id:string;project_id:string;name:string;scan_type:string;status:string;progress?:number|null;current_phase?:string|null;security_score?:number|null;risk_level?:string|null;findings_count?:number|null;created_at:string;completed_at?:string|null}
type Project={id:string;name:string}
type Asset={id:string;project_id:string;name:string;type:string;configuration:Record<string,unknown>;is_active:boolean}
type Engine={id?:string;name?:string;slug?:string;enabled?:boolean}

export const ScanPage=()=>{
 const t=useLanguageStore(s=>s.t); const qc=useQueryClient(); const [open,setOpen]=useState(false); const [projectId,setProjectId]=useState(''); const [assetId,setAssetId]=useState(''); const [engine,setEngine]=useState(''); const [name,setName]=useState(''); const [busy,setBusy]=useState(false)
 const scans=useQuery<Scan[]>({queryKey:['scans'],queryFn:()=>apiHelpers.get<Scan[]>('/scans/?limit=100')})
 const projects=useQuery<Project[]>({queryKey:['projects'],queryFn:()=>apiHelpers.get<any>('/projects/')})
 const assets=useQuery<Asset[]>({queryKey:['assets-scan-picker',projectId],queryFn:()=>apiHelpers.get<Asset[]>('/assets/',{params:{project_id:projectId}}),enabled:Boolean(projectId)})
 const engines=useQuery<Engine[]>({queryKey:['engines'],queryFn:()=>apiHelpers.get<Engine[]>('/engines'),staleTime:60_000})
 const selectedAsset=assets.data?.find(a=>a.id===assetId)
 const create=async()=>{if(!projectId||!name.trim()||!engine)return;setBusy(true);try{const target=selectedAsset?.configuration?.url||selectedAsset?.configuration?.host||selectedAsset?.configuration?.ip||selectedAsset?.configuration?.domain||selectedAsset?.configuration?.repo_url||selectedAsset?.configuration?.path||selectedAsset?.name;const scanType=selectedAsset?.type==='website'||selectedAsset?.type==='domain'?'url':selectedAsset?.type==='network_range'?'network':selectedAsset?.type==='source_code'?'code':selectedAsset?.type==='file'?'file':selectedAsset?.type==='docker_image'?'docker':selectedAsset?.type==='api_endpoint'?'api':'ip';const result=await apiHelpers.post<Scan>('/scans/',{project_id:projectId,name:name.trim(),scan_type:scanType,asset_id:assetId||undefined,engines:[engine],depth:'standard',config:target?{target}: {},authorized:true});await qc.invalidateQueries({queryKey:['scans']});setOpen(false);setName('');toast.success(`${t('Scan queued')} · ${result.id}`)}catch(e:any){toast.error(e?.response?.data?.detail||e?.message||t('Unable to create scan'))}finally{setBusy(false)}}
 const remove=async(id:string)=>{if(!window.confirm(t('Delete this scan?')))return;try{await apiHelpers.delete(`/scans/${id}`);await qc.invalidateQueries({queryKey:['scans']});toast.success(t('Scan deleted'))}catch(e:any){toast.error(e?.response?.data?.detail||e?.message||t('Unable to delete scan'))}}
 return <div className="space-y-6 pb-10"><section className="enterprise-card rounded-3xl p-6 md:p-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[0.16em] text-primary"><Activity className="h-4 w-4"/>{t('Validations')}</div><h1 className="mt-2 text-3xl font-semibold tracking-tight">{t('Scan registry')}</h1><p className="mt-2 text-sm text-muted-foreground">{t('Every execution shown here is a persisted Scan and engine execution from the platform backend.')}</p></div><div className="flex gap-2"><button type="button" onClick={()=>scans.refetch()} className="rounded-xl border p-2.5"><RefreshCw className={cn('h-4 w-4',scans.isFetching&&'animate-spin')}/></button><button type="button" onClick={()=>setOpen(true)} className="inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground"><Plus className="h-4 w-4"/>{t('New scan')}</button></div></div></section>{scans.isLoading?<div className="enterprise-card p-12 text-center text-sm text-muted-foreground">{t('Loading...')}</div>:scans.isError?<div className="enterprise-card p-12 text-center"><AlertCircle className="mx-auto h-8 w-8 text-destructive"/><p className="mt-3 font-medium">{t('Unable to load scans')}</p><button onClick={()=>scans.refetch()} className="mt-4 rounded-xl border px-4 py-2 text-sm">{t('Retry')}</button></div>:<section className="enterprise-card overflow-hidden rounded-2xl"><div className="overflow-x-auto"><table className="w-full min-w-[1080px] text-sm"><thead><tr className="border-b bg-muted/20 text-xs text-muted-foreground"><th className="px-4 py-3 text-start">{t('Scan')}</th><th className="px-4 py-3 text-start">{t('Type')}</th><th className="px-4 py-3 text-start">{t('Status')}</th><th className="px-4 py-3 text-start">{t('Progress')}</th><th className="px-4 py-3 text-start">{t('Security score')}</th><th className="px-4 py-3 text-start">{t('Risk')}</th><th className="px-4 py-3 text-start">{t('Findings')}</th><th className="px-4 py-3 text-start">{t('Created')}</th><th className="px-4 py-3 text-start">{t('Actions')}</th></tr></thead><tbody>{!scans.data?.length?<tr><td colSpan={9} className="px-6 py-16 text-center"><Activity className="mx-auto h-7 w-7 text-muted-foreground"/><p className="mt-3 font-medium">{t('No scans available')}</p></td></tr>:scans.data.map(scan=><tr key={scan.id} className="border-b last:border-0 hover:bg-muted/20"><td className="px-4 py-4"><Link to={`/scan/${scan.id}/results`} className="font-semibold hover:text-primary">{scan.name}</Link><div className="mt-1 font-mono text-[10px] text-muted-foreground">{scan.id}</div></td><td className="px-4 py-4">{scan.scan_type}</td><td className="px-4 py-4"><span className="rounded-full border bg-muted/30 px-2 py-1 text-[11px]">{scan.status}</span></td><td className="px-4 py-4">{scan.progress==null?'—':`${scan.progress}%`}{scan.current_phase?<div className="text-[10px] text-muted-foreground">{scan.current_phase}</div>:null}</td><td className="px-4 py-4">{scan.security_score==null?'—':scan.security_score}</td><td className="px-4 py-4">{scan.risk_level||'—'}</td><td className="px-4 py-4">{scan.findings_count==null?'—':scan.findings_count}</td><td className="px-4 py-4 text-xs text-muted-foreground">{new Date(scan.created_at).toLocaleString()}</td><td className="px-4 py-4"><div className="flex gap-1"><Link to={`/scan/${scan.id}/results`} className="rounded-lg p-2 hover:bg-accent" title={t('Open')}><ArrowUpRight className="h-4 w-4"/></Link><Link to={`/scan/${scan.id}/progress`} className="rounded-lg p-2 text-primary hover:bg-primary/10" title={t('Progress')}><Play className="h-4 w-4"/></Link><button type="button" onClick={()=>remove(scan.id)} className="rounded-lg p-2 text-destructive hover:bg-destructive/10" title={t('Delete')}><Trash2 className="h-4 w-4"/></button></div></td></tr>)}</tbody></table></div></section>}{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/55 p-4 backdrop-blur-sm"><div className="enterprise-card w-full max-w-xl rounded-3xl p-6"><div className="flex items-center justify-between"><div><h2 className="text-xl font-semibold">{t('New scan')}</h2><p className="mt-1 text-xs text-muted-foreground">{t('Execution is created server-side and remains subject to authorization and scope controls.')}</p></div><button onClick={()=>setOpen(false)} className="rounded-xl border p-2"><X className="h-4 w-4"/></button></div><div className="mt-6 space-y-4"><label className="block"><span className="text-sm font-medium">{t('Project')}</span><select value={projectId} onChange={e=>{setProjectId(e.target.value);setAssetId('')}} className="mt-2 h-11 w-full rounded-xl border bg-background px-3 text-sm"><option value="">{t('Select project')}</option>{(projects.data||[]).map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label className="block"><span className="text-sm font-medium">{t('Asset')}</span><select value={assetId} onChange={e=>setAssetId(e.target.value)} disabled={!projectId} className="mt-2 h-11 w-full rounded-xl border bg-background px-3 text-sm"><option value="">{t('Select asset')}</option>{(assets.data||[]).filter(a=>a.is_active).map(a=><option key={a.id} value={a.id}>{a.name} · {a.type}</option>)}</select></label><label className="block"><span className="text-sm font-medium">{t('Scan name')}</span><input value={name} onChange={e=>setName(e.target.value)} className="mt-2 h-11 w-full rounded-xl border bg-background px-3 text-sm"/></label><label className="block"><span className="text-sm font-medium">{t('Engine')}</span><select value={engine} onChange={e=>setEngine(e.target.value)} className="mt-2 h-11 w-full rounded-xl border bg-background px-3 text-sm"><option value="">{t('Select engine')}</option>{(engines.data||[]).filter(e=>e.enabled!==false).map(e=>{const id=String(e.slug||e.name||e.id||'');return <option key={id} value={id}>{id}</option>})}</select></label></div><div className="mt-7 flex justify-end gap-2"><button onClick={()=>setOpen(false)} className="rounded-xl border px-4 py-2.5 text-sm">{t('Cancel')}</button><button onClick={create} disabled={busy||!projectId||!name.trim()||!engine} className="rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground disabled:opacity-50">{busy?t('Creating…'):t('Create scan')}</button></div></div></div>}</div>
