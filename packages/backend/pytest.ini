[pytest]
DJANGO_SETTINGS_MODULE = django_project.settings
python_files = test_*.py *_test.py
testpaths = tests
asyncio_mode = auto
markers =
    integration: integration/runtime tests
    timeout: tests with an execution timeout
