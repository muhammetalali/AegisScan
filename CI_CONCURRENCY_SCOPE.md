# CI concurrency migration scope

The first hardened tranche covers Domain Contract Reality and Frontend Lock Sync, plus dedicated CI Orchestration, Scanner Reliability, Domain Integrity, and Reality policy gates. Supply Chain Release remains explicitly non-cancellable.

The policy inventory deliberately fails while any other workflow whose displayed name contains `Reality` lacks an explicit concurrency policy. This makes the remaining migration mechanically visible instead of silently declaring the queue-hardening phase complete.
