# Retained report examples

The Markdown files in this directory are historical audit examples, not live application state. New runtime output belongs under `aegis-platform/backend/runtime/reports/`, which is ignored by Git. Keep report content free of credentials and personal data before committing it.
