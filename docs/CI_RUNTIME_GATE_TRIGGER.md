# Backend Runtime Gate Trigger

This marker exists only to trigger the backend runtime CI gate after the verified local security baseline.

The authoritative runtime contract is defined by `.github/workflows/backend-runtime-gate.yml`.
